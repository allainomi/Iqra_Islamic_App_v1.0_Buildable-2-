android {
    compileSdkVersion 33
    // ... دیگر ترتیبات

    defaultConfig {
        minSdkVersion 21
        targetSdkVersion 33
        // ... دیگر ترتیبات
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
            // shrinkResources false  // اگر ضرورت ہو تو
            // minifyEnabled false    // اگر ضرورت ہو تو
        }
    }
}
