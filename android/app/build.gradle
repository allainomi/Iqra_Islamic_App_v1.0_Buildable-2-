buildscript {
    ext {
        buildToolsVersion = "33.0.0"
        minSdkVersion = 21
        compileSdkVersion = 33
        targetSdkVersion = 33
        // یہ لائن شامل کریں:
        kotlin_version = "1.8.0" // یا تازہ ترین ورژن
        
        // اگر آپ React Native 0.71+ استعمال کر رہے ہیں:
        // kotlinVersion = "1.8.0"
    }
    // ... باقی کوڈ
}
