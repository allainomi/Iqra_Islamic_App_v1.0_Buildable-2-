buildscript {
    ext {
        buildToolsVersion = "33.0.0"
        minSdkVersion = 21
        compileSdkVersion = 33
        targetSdkVersion = 33
    }
    // ... ڈیپنڈنسیز
}

// اگر یہ بلاک نہیں ہے تو شامل کریں:
ext {
    kotlin_version = "1.8.0"
}
